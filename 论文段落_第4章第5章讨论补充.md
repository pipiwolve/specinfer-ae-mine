# 第4章 / 第5章讨论部分可直接粘贴段落

## 一、第4章讨论部分补充：单卡常驻 resident 路线的结果解释

### 4.X 单卡常驻场景下未观察到稳定加速的原因分析

本文在 AutoDL `A100-PCIE-40GB x1` 环境中对单卡常驻 resident 路线进行了多轮验证，包括 `LLaMA-7B + LLaMA-68M`、`LLaMA-7B + LLaMA-160M` 以及对齐早期脚本参数的 round-aligned benchmark。实验结果表明，在当前 A100 环境下，`sequence speculative inference` 与 `tree speculative inference` 均未优于 `incremental decoding`，且对应的 `verified mean` 长期接近 `1`。这一现象说明，在单卡常驻 `7B` 模型的场景中，推测推理虽然能够正常运行，但每轮仅能额外验证极少量 token，难以有效减少目标模型的完整解码步数，因此 speculative 路径引入的草稿模型生成、验证与调度开销无法被摊薄，最终表现为端到端延迟劣于 baseline。

从系统机制上看，resident 与 offloading 的瓶颈结构并不相同。在 resident 模式下，目标模型参数可以稳定驻留于 GPU 显存，参数传输开销较低，系统主要瓶颈转向目标模型本身的计算过程以及 speculative verification 的额外调度成本。因此，只有当 speculative inference 能够在单次验证中确认显著多于 `1` 个 token 时，推测推理路径才可能转化为端到端收益。当前 A100 单卡实验未满足这一条件，因此未观察到加速结果。

### 4.X 早期 RTX 4090 resident 正结果的定位与解释

需要指出的是，本文在早期探索阶段曾在 `RTX 4090 24GB` 环境下观察到 resident 路线的正向趋势，即 `sequence` 或 `tree` 路径在部分 batch size 下优于 `incremental decoding`，同时对应的 verified token 统计也高于最终 A100 resident 实验。该现象说明 resident 路线并非在所有硬件条件下都无效，而是具有明显的环境依赖性。与 A100 40GB 相比，4090 24GB 的显存余量更为紧张，在模型权重、KV cache、batch 以及运行时缓冲共同竞争显存资源时，减少目标模型完整解码步数所带来的收益更容易显现。

然而，本文并未将该 RTX 4090 resident 结果作为正文主结论，原因在于该批实验属于早期探索性结果，缺少与最终 AutoDL 实验相同粒度的环境快照、脚本版本与 manifest 记录，且未在当前 A100 环境中稳定复现。因此，从论文严谨性出发，正文主结论仍以当前可重复导出的 A100 resident 结果为准，而 RTX 4090 resident 结果更适合作为补充观察，用于说明 speculative inference 在单卡常驻场景中的收益具有显著的硬件与资源条件依赖性。

### 4.X 本章结论

综合来看，单卡常驻 resident 路线的实验结果揭示了 SpecInfer 的一个重要边界条件：当目标模型能够稳定驻留于 GPU，且 speculative verification 每轮仅能确认极少 token 时，推测推理的额外开销可能超过其节省的解码成本，从而难以形成正向收益。另一方面，RTX 4090 探索性结果又表明 resident 路线并非绝对无效，而是对显存余量、运行时分配策略与 verified token 数量高度敏感。因此，resident 实验在本文中的主要作用并非给出最强加速结论，而是用于揭示 SpecInfer 的适用边界与收益条件。

---

## 二、第5章讨论部分补充：offloading 路线为何获得显著正向收益

### 5.X 单卡 offloading 场景下加速结果的原因分析

与 resident 场景不同，单卡 offloading 路线的核心瓶颈不再主要来自目标模型的计算本身，而是来自 CPU 与 GPU 之间的大模型参数搬运成本。在 `OPT-13B + OPT-125M` 的单卡 offloading 配置下，目标模型参数无法长期完全驻留于 GPU，需要在推理过程中频繁从 CPU 或 zero-copy memory 迁移至 GPU 执行解码。此时，若 speculative inference 能够减少目标模型的完整解码步数，则不仅减少了目标模型计算次数，也直接减少了昂贵的参数传输次数，因此端到端收益会被显著放大。

本文的 offloading 实验结果验证了这一点。在 `batch size = 1, 2, 4` 的设置下，SpecInfer 相对于 offloading baseline 分别取得约 `3.85x`、`3.84x` 与 `3.93x` 的 speedup，且多组日志中观测到 `verified tokens > 1` 的情况显著增加，平均 verified token 数明显高于 resident 实验中的 `1.0` 水平。这说明 speculative inference 在 offloading 场景下确实改变了目标模型的有效解码行为，而非仅仅增加额外开销。

### 5.X 与 resident 路线的对比讨论

将本章 offloading 结果与上一章 resident 结果对照，可以更清楚地理解 SpecInfer 的适用条件。resident 场景中，目标模型参数已位于 GPU，传输瓶颈较弱，speculative 路径只有在 verified token 足够多时才可能获益；而 offloading 场景中，目标模型每一步都伴随着显著的参数调度与搬运成本，因此即使 speculative inference 只减少有限数量的目标模型解码步，也能够带来较为明显的总延迟下降。换言之，SpecInfer 的收益并不是一个与硬件和部署方式无关的常数，而是会随着系统瓶颈位置的变化而变化。当瓶颈主要来自参数搬运时，SpecInfer 更容易产生显著正向效果；当瓶颈主要来自模型本身计算且 verified token 数不足时，其收益则会明显减弱。

### 5.X 与早期 resident 正结果的统一解释

若将本章 offloading 结果与前文提到的 RTX 4090 resident 探索性正结果结合起来，可以得到更完整的解释框架：SpecInfer 并非只在“多机”或“超大模型”场景下有效，而是在所有“减少目标模型完整解码步数能够显著降低主导瓶颈”的场景下都可能有效。对于 RTX 4090 resident，瓶颈更可能来自较紧张的显存余量和更敏感的运行时开销；对于 A100 offloading，瓶颈更明确地来自 CPU-GPU 参数搬运。因此，这两类正结果虽然出现在不同实验形态下，但其底层共同点都是 speculative inference 成功减少了目标模型的高成本执行步骤。

### 5.X 本章结论

本章结果表明，单卡 offloading 是本文中最能体现 SpecInfer 优势的实验场景。与 resident 路线相比，offloading 场景不仅与原论文强调的收益区间更一致，也更清楚地展示了 verified token 数量增长如何转化为端到端加速。因而，本文最终将 offloading 路线作为核心正向实验结果，将其作为判断 SpecInfer 在单卡资源受限条件下是否“有用”的主要依据。

---

## 三、若需要更强烈地衔接第4章与第5章，可加入的过渡段

上一章 resident 实验表明，SpecInfer 的收益并不会在所有单卡推理场景中自然出现。当目标模型能够稳定驻留于 GPU、且 speculative verification 每轮只能确认极少 token 时，推测推理难以产生端到端收益。然而，这并不意味着 SpecInfer 本身无效，而是说明其收益具有明显的部署条件依赖性。因此，本文进一步转向更符合原论文优势区间的 offloading 场景，检验在参数搬运成本占主导的单卡部署模式下，SpecInfer 是否能够释放其潜在收益。下一章的实验结果表明，这一判断是成立的。
