# 摘要

随着大型语言模型在问答系统、检索增强生成和专业领域推理中的广泛应用，推理阶段的时间成本与资源开销逐渐成为系统落地的主要瓶颈。推测解码（Speculative Decoding）通过引入小型草稿模型预生成候选 token，并由大型目标模型进行验证，从而减少目标模型逐 token 解码次数，是近年来重要的推理加速方向。SpecInfer 在此基础上进一步提出 token tree 与 tree-based verification，重点面向分布式和 offloading 两类高系统开销场景提升大模型推理效率。

本文围绕 SpecInfer 在受限单卡环境下的复现与评估展开研究。首先，本文在 AutoDL 单卡 A100 环境中完成了 SpecInfer AE 的工程复现，解决了 FlexFlow/Legion 依赖、UCX 与 MPI 构建、Rust 编译、模型缓存路径、tokenizer 同步及实验记录等一系列工程问题，建立了可中断续跑、可自动汇总、可导出结果的单卡实验流程。随后，本文设计了两条实验路线：其一为单卡常驻推理实验，以 `LLaMA-7B` 为目标模型、`LLaMA-68M/160M` 为草稿模型，对比 `incremental decoding`、`sequence-based speculative inference` 和 `tree-based speculative inference`；其二为单卡 offloading 推理实验，以 `OPT-13B` 为目标模型、`OPT-125M` 为草稿模型，对比 target-only offloading baseline 与 speculative offloading。

实验结果表明，SpecInfer 在不同系统瓶颈下的收益存在显著差异。在单卡常驻场景中，虽然 speculative 推理链路能够稳定运行，但其平均 verified token 长期约等于 `1`，未能转化为速度收益；以英文 `len64` 工作负载为例，speculative 模式的平均延迟反而高于 incremental baseline。在单卡 offloading 场景中，SpecInfer 则表现出稳定的正向收益：在 `OPT-13B + OPT-125M` 配置下，`batch size = 1/2/4` 的平均 speedup 分别达到 `3.85x`、`3.84x` 和 `3.93x`。进一步统计表明，该场景下 speculative 路径的平均 verified token 数达到 `4.09`，中位数为 `3`，最大值为 `9`，显著高于单卡常驻场景。

本文的研究表明，SpecInfer 的收益高度依赖系统级瓶颈与每轮可验证 token 数量：在目标模型常驻 GPU、真实解码成本较低且验证 token 数不足的场景中，其额外开销难以被摊薄；而在 offloading 这类存在显著 CPU-GPU 参数调度开销的场景中，SpecInfer 则能够稳定发挥加速作用。本文还结合英文医疗问答 workload 对推测推理的稳定性和应用迁移边界进行了分析，为后续在受限单卡环境下部署推测推理系统提供了工程经验与实证依据。

**关键词：** SpecInfer；推测解码；单卡复现；offloading 推理；大语言模型；医疗问答

---

# 第2章 相关工作

## 2.1 推测解码与 Speculative Decoding

推测解码（Speculative Decoding）是近年来大模型推理优化中最具代表性的方向之一。其核心思想是利用一个较小、较快的草稿模型先对目标模型未来可能生成的 token 进行预测，再由目标模型对这些候选结果进行验证。与传统逐 token 顺序生成相比，该方法在草稿模型质量较高、候选前缀与目标模型分布接近时，可以显著减少目标模型的真实解码步数，从而降低整体延迟。

Leviathan 等人的工作奠定了这一方向的理论与实验基础。该类方法通常采用两阶段验证与拒绝采样框架，保证在一定条件下输出分布与目标模型保持一致，并分析了接受率、草稿步长和整体加速比之间的关系。后续研究进一步从理论角度讨论了 speculative decoding 的最优性边界，指出草稿模型的质量、任务分布和实现细节将共同决定实际收益。也就是说，推测解码并不是一种无条件成立的加速手段，其有效性依赖于草稿模型与目标模型之间的匹配程度，以及底层推理系统中的真实瓶颈。

在这一背景下，接受率与验证 token 数量成为推测解码研究中两个最重要的解释变量。如果草稿模型提出的候选 token 多数能够被目标模型接受，那么目标模型需要执行的真实解码步数将明显减少，进而转化为延迟收益；反之，如果草稿模型大多数候选均被拒绝，则 speculative 路径会退化为额外开销。因此，推测解码研究不仅关注最终速度，也强调对“每轮到底确认了多少 token”进行细粒度分析。

## 2.2 SpecInfer 与系统级推测推理

SpecInfer 在传统 speculative decoding 的基础上，进一步面向系统级推理瓶颈提出了更复杂的验证结构。与序列式 speculative decoding 只沿单一路径验证候选 token 不同，SpecInfer 构建 token tree，在一次验证中同时覆盖多个可能前缀，从而尽量提升每轮验证的 token 数量。该设计使得 SpecInfer 不再仅仅是“多跑一个小模型换少跑几个大模型”的简单扩展，而是成为一种针对系统级延迟构成进行优化的推理框架。

SpecInfer 论文特别强调，其最强收益主要出现在两类场景中。第一类是分布式大模型推理，此时目标模型的瓶颈不仅来自计算，还来自跨设备激活传输与同步；第二类是 offloading 推理，此时目标模型参数需在 CPU 与 GPU 之间频繁调度和搬运，真实解码步数的减少能够直接转化为系统级时间节省。换言之，SpecInfer 的价值并不只是“减少大模型计算”，更在于当大模型推理瓶颈主要表现为系统级数据移动时，speculative verification 可以明显放大收益。

与此同时，SpecInfer 也提示了一个重要事实：tree-based speculative inference 并不必然优于 sequence-based speculative inference。在系统实现中，tree-based 路径需要更多验证结构、更多 bookkeeping 和更复杂的 batch 管理；如果平均每轮只能确认极少 token，那么 tree 的附加开销有可能抵消其理论优势。因此，在实验分析中，除了比较最终延迟，还需要比较 sequence 与 tree 的 verified token 分布，以及它们与任务负载之间的关系。

## 2.3 推测推理的性能影响因素

已有系统研究表明，推测推理的实际收益受多种因素共同影响，不能简单理解为“草稿模型越强、加速比越高”或“tree 一定优于 sequence”。其主要影响因素包括以下几方面。

第一，草稿模型与目标模型之间的匹配程度。草稿模型过小会导致提出的候选 token 准确性不足，从而降低验证通过概率；草稿模型过大则会增加草稿阶段本身的计算和系统开销，可能削弱最终收益。

第二，任务工作负载形态。不同 prompt 类型、不同语言、不同输出长度、不同 batch size 都会影响 speculative verification 的收益。例如，较短回答预算下，目标模型本身的逐 token 计算开销有限，草稿生成与验证的附加成本更容易主导总延迟；而较长输出或更重的系统搬运瓶颈则更有利于 speculative 机制发挥作用。

第三，系统实现细节。接受率或 verified token 数量只是算法层面的潜力指标，是否能够转化为端到端加速，还取决于底层框架实现、内存管理、并发调度、batch 组织、缓存布局等因素。也正因为如此，SpecInfer 这类系统级推测推理方法需要在具体部署环境中进行实证评估，而不能仅凭算法层面的直觉做出结论。

## 2.4 RAG、医疗问答与应用迁移背景

检索增强生成（Retrieval-Augmented Generation, RAG）已经成为提升大模型在专业领域问答中表现的重要范式。在医疗问答等高专业性场景中，单纯依赖基础大模型往往难以保证知识时效性与证据可追踪性，因此通常需要通过文档检索、知识增强、重排序与上下文拼接来提升回答质量。相关研究指出，高质量检索、上下文截断与引用控制，是医疗场景 RAG 系统中最关键的工程问题之一。

另一方面，随着多智能体和 Agent 系统的发展，大模型在实际系统中的角色已不再局限于“单轮问答器”，而逐渐承担任务分解、工具调用、知识整合和多轮协作等角色。在这一背景下，底层推理速度的提升将直接影响上层业务系统的可用性和交互体验。换言之，推测推理虽然属于底层系统优化问题，但其应用价值最终要通过上层问答系统、RAG 链路或 Agent 系统的落地场景来体现。

本文在实验设计中并未将医疗问答作为主性能实验对象，而是将其作为应用迁移背景纳入考察。这一处理方式的原因在于：SpecInfer 的主收益区间首先取决于部署方式与验证 token 数量，而医疗问答更多承担的是 workload 层面的稳定性与迁移性分析角色。通过将英文医疗问答 workload 与单卡常驻、offloading 两类实验区分对待，本文试图同时回答“系统是否有效”和“应用迁移是否可行”这两个层面的问题。

## 2.5 本章小结

本章从四个方面回顾了本文相关研究背景：首先介绍了 speculative decoding 的基本思想及其理论基础；其次梳理了 SpecInfer 面向系统瓶颈的 token tree 与 tree-based verification 框架；再次总结了推测推理的主要性能影响因素；最后说明了医疗问答与系统接入场景在本文中的定位。上述分析为后续章节中的系统设计、实验对照和结果解释提供了理论基础。

---

# 第4章 单卡常驻场景实验与分析

## 4.1 实验目的与设置

本章主要分析 SpecInfer 在单卡常驻推理场景下的表现。该场景中，目标模型已常驻 GPU 显存，目标模型真实解码不涉及显著的 CPU-GPU 参数搬运，因此更适合用于考察以下问题：在没有明显参数搬运瓶颈的条件下，SpecInfer 的 speculative 机制是否仍然能够带来稳定加速？

为回答这一问题，本文基于官方 `server_gpu_experiments.sh` 的单卡部分构建实验，对比以下三类模式：

1. `incremental decoding`
2. `sequence-based speculative inference`
3. `tree-based speculative inference`

实验使用的目标模型与草稿模型组合包括：

- `LLaMA-7B + LLaMA-68M`
- `LLaMA-7B + LLaMA-160M`

在 benchmark 配置上，本文重点考察 `batch size = 1/2/4`，并采用与官方脚本一致的英文 continuation 风格 prompt。作为补充，本文还引入了英文医疗问答 `len64` workload，用于观察在较短回答预算下 speculative 与 baseline 的稳定性与延迟差异。

## 4.2 单卡常驻 benchmark 结果

> [图4-1 插入位置]  
> 图4-1 单卡常驻场景下三种推理模式的平均延迟对比  
> 数据来源：`final_review_20260316_035130/server_gpu_single_a100/summary.csv`  
> 对应图片：`论文图表/fig_4_1_resident_latency.png`

> [表4-1 插入位置]  
> 表4-1 单卡常驻 benchmark 主要结果汇总  
> 建议字段：batch size、mode、num_profiles、latency_mean_us、latency_p50_us、verified_mean。


单卡常驻 benchmark 的主要结果见 [summary.csv](/Users/Zhuanz/Documents/Typora/面经整理/面试/计算机/fresh/specinfer-ae/final_review_20260316_035130/server_gpu_single_a100/summary.csv)。从 `LLaMA-7B + LLaMA-68M` 的结果可以看出，在 `batch size = 1` 条件下：

- `incremental decoding` 的平均延迟约为 `1.64 ms`
- `sequence-based speculative inference` 的平均延迟约为 `5.06 ms`
- `tree-based speculative inference` 的平均延迟约为 `4.95 ms`

当 `batch size` 增大到 `2` 和 `4` 时，整体趋势保持一致：speculative 两条路径均明显慢于 incremental baseline。值得注意的是，`tree-based` 路径在 `batch size = 4` 下只记录到部分 profile，说明当前实现与该配置下的 batch capacity 存在边界问题，但即便忽略这一点，其平均延迟仍未显示出优于 incremental 的趋势。

进一步观察 verified token 指标可以发现，单卡常驻 benchmark 中无论是 `sequence` 还是 `tree`，其 `verified_mean` 基本都等于 `1.000`。也就是说，在当前实验配置下，speculative 路径虽然确实在运行，但平均每轮几乎只能确认 1 个 token，并没有形成论文中所强调的“每轮确认多个 token，从而显著减少目标模型真实解码步数”的效果。

## 4.3 更强草稿模型的补充实验

为了确认负结果是否仅仅来自草稿模型过小，本文进一步将草稿模型由 `68M` 提升至 `160M`，并补做了 `batch size = 1` 的 focused benchmark。结果见 [focus_160m/summary.csv](/Users/Zhuanz/Documents/Typora/面经整理/面试/计算机/fresh/specinfer-ae/final_review_20260316_035130/server_gpu_single_a100/focus_160m/summary.csv)。

实验结果表明：

- `incremental decoding` 的平均延迟约为 `1.66 ms`
- `sequence-based speculative inference` 的平均延迟约为 `8.30 ms`
- `tree-based speculative inference` 的平均延迟约为 `9.74 ms`

与此同时，`verified_mean` 依然为 `1.000`。这说明，单卡常驻负结果并不能简单归因于“68M 草稿模型太弱”。即使将草稿模型提升到 `160M`，在当前工作负载与单卡部署条件下，verified token 数仍未上升到足以支撑 speculative 收益的区间。

## 4.4 英文医疗问答 `len64` 补充实验

为了观察 speculative 推理在更接近应用场景的短回答任务中是否会出现不同表现，本文进一步构造了 50 条英文医疗问答数据，并在 `max-length = 64` 的稳定配置下比较 incremental baseline 与 speculative inference。

稳定性结果见 [failure_summary.txt](/Users/Zhuanz/Documents/Typora/面经整理/面试/计算机/fresh/specinfer-ae/final_review_20260316_035130/english_len64/stability_summary_en/failure_summary.txt)。实验表明：

- `incr`：50 条中 `46` 条为 `ok`，`4` 条为 `truncated`
- `spec`：50 条中 `46` 条为 `ok`，`4` 条为 `truncated`

说明在英文短回答工作负载下，两种模式都具有较好的稳定性，且没有出现大量 runtime error 或明显乱码。

然而，延迟结果见 [latency_summary.txt](/Users/Zhuanz/Documents/Typora/面经整理/面试/计算机/fresh/specinfer-ae/final_review_20260316_035130/english_len64/latency_analysis_en/latency_summary.txt)：

- `baseline_mean = 0.936`
- `spec_mean = 1.213`
- `average_speedup = 0.477`

这意味着 speculative 模式在该工作负载上不仅没有加速，反而慢于 baseline。接受率代理指标见 [acceptance_summary.txt](/Users/Zhuanz/Documents/Typora/面经整理/面试/计算机/fresh/specinfer-ae/final_review_20260316_035130/english_len64/acceptance_analysis_en/acceptance_summary.txt)，其 `mean_acceptance_ratio = 0.991`、`median_acceptance_ratio = 1.0`，说明两者最终输出前缀较为一致，但该指标属于输出一致性代理，并不能替代论文中的真实 verified token 统计。因此，这组实验更多说明的是：在短回答应用负载下，speculative 机制可稳定运行，但不足以带来实际速度收益。

## 4.5 原因分析

结合上述结果，本文认为单卡常驻场景未观察到加速的原因主要有以下四点。

第一，目标模型常驻 GPU 时，系统瓶颈主要仍然集中在目标模型本身的逐 token 推理，而非大规模参数搬运。此时 speculative 路径能够节省的“系统级搬运时间”有限，因此其收益主要取决于 verified token 数是否足够高。

第二，当前实验中 `verified_mean` 长期约等于 `1.0`，说明 speculative 路径几乎没有减少目标模型真实解码步数。由于目标模型仍需要频繁执行验证，而 speculative 路径还额外引入了草稿模型生成、验证调度和 bookkeeping 等开销，因此整体延迟自然高于 incremental baseline。

第三，短回答和较短 prompt 预算进一步削弱了 speculative 的收益空间。在 `len64` 工作负载中，单次回答本身很短，baseline 的绝对延迟已经不高，speculative 的附加开销更容易成为主导。

第四，当前工作负载和模型组合下，草稿模型与目标模型之间的分布匹配程度不足以显著抬高 verified token 数。无论是 `68M` 还是 `160M`，其在单卡常驻场景下都未体现出“多 token 并行确认”的机制优势。

## 4.6 本章小结

本章对单卡常驻场景下的 SpecInfer 进行了系统评估。实验表明，在 `LLaMA-7B` 常驻 GPU 的条件下，SpecInfer 的 speculative 路径能够稳定运行，但无论是 `68M` 还是 `160M` 草稿模型，平均每轮确认的 token 数均接近 `1`，未能转化为端到端加速。进一步地，在英文医疗问答短回答工作负载中，speculative 模式虽然保持了较高稳定性，但总体延迟仍高于 baseline。上述结果说明，SpecInfer 的收益并非在所有单卡场景中都成立，其效果高度依赖于具体部署瓶颈与验证 token 分布。

---

# 第5章 单卡 Offloading 场景实验与分析

## 5.1 实验目的与设置

与上一章的单卡常驻场景不同，本章关注单卡 offloading 场景。在该场景中，目标模型部分参数需要在 CPU 与 GPU 之间进行调度和传输，因此目标模型真实解码步数的减少理论上更容易转化为系统级时间收益。这也是 SpecInfer 论文中明确强调的主要正向区间之一。

为尽量与官方 offloading 实验保持一致，本文采用以下模型组合：

- 目标模型：`facebook/opt-13b`
- 草稿模型：`facebook/opt-125m`

并比较两种运行模式：

1. **target-only offloading baseline**
2. **sequence-based speculative offloading**

在实验参数上，考虑到 AutoDL 单卡环境的资源限制，本文采用收敛后的稳定参数：

- `CPUS = 4`
- `UTILS = 4`
- `ZSIZE = 65000`
- `MAX_SEQ_LEN = 128`
- `batch size = 1/2/4`

其中 speculative 路线与 baseline 路线均使用同一组 prompt 文件 `chatgpt_offloading.json`，以保证比较公平。

## 5.2 baseline 与 speculative 的端到端对比

> [图5-1 插入位置]  
> 图5-1 Offloading 场景下 baseline 与 speculative 的平均 speedup 对比  
> 数据来源：`final_review_20260316_035130/offloading/compare.csv`  
> 对应图片：`论文图表/fig_5_1_offloading_speedup.png`

> [图5-2 插入位置]  
> 图5-2 Offloading 场景下 baseline 与 speculative 的平均延迟对比  
> 数据来源：`final_review_20260316_035130/offloading/compare.csv`  
> 对应图片：`论文图表/fig_5_2_offloading_latency.png`

> [表5-1 插入位置]  
> 表5-1 单卡 offloading baseline 与 speculative 对比结果  
> 建议字段：batch size、baseline_mean_us、baseline_p50_us、spec_mean_us、spec_p50_us、speedup_mean、speedup_p50。


offloading 实验的主要对比结果见 [compare.csv](/Users/Zhuanz/Documents/Typora/面经整理/面试/计算机/fresh/specinfer-ae/final_review_20260316_035130/offloading/compare.csv)。实验表明：

- `batch size = 1` 时，平均 speedup 为 `3.85x`
- `batch size = 2` 时，平均 speedup 为 `3.84x`
- `batch size = 4` 时，平均 speedup 为 `3.93x`

与此同时，baseline 和 speculative 两组实验在 `batch = 1/2/4` 上均成功完成，没有出现 runtime failure。相应的汇总结果分别见：

- [baseline summary.csv](/Users/Zhuanz/Documents/Typora/面经整理/面试/计算机/fresh/specinfer-ae/final_review_20260316_035130/offloading/baseline_opt13b_baseline_r1/summary.csv)
- [spec summary.csv](/Users/Zhuanz/Documents/Typora/面经整理/面试/计算机/fresh/specinfer-ae/final_review_20260316_035130/offloading/spec_opt13b_r1/summary.csv)

从具体数值看，baseline 的平均延迟大约稳定在 `238s` 左右，而 speculative offloading 的平均延迟则约为 `61s`。换言之，在当前单卡 offloading 场景下，引入 `OPT-125M` 草稿模型后，SpecInfer 成功将端到端延迟降低到 baseline 的约四分之一。

这一结果与前一章单卡常驻场景形成鲜明对比：在常驻场景中，speculative 路径始终慢于 baseline，而在 offloading 场景中，speculative 路径则表现出稳定且显著的正向收益。

## 5.3 机制解释：verified token 分布

> [图5-3 插入位置]  
> 图5-3 Offloading speculative 路径的 verified token 分布  
> 数据来源：`final_review_20260316_035130/offloading/spec_opt13b_r1/offloading_small_*.out`  
> 对应图片：`论文图表/fig_5_3_offloading_verified_distribution.png`

> [表5-2 插入位置]  
> 表5-2 Offloading verified token 统计结果  
> 建议字段：count、mean_verified_tokens、median_verified_tokens、max_verified_tokens、gt1_ratio。


为了进一步解释为什么 offloading 路线能够获得显著加速，本文对 speculative offloading 的原始 `.out` 日志进行了额外统计。结果表明：

- 平均 `verified tokens = 4.09`
- 中位数 `3`
- 最大值 `9`
- `verified tokens > 1` 的比例约为 `71.3%`

这一统计结果非常关键。它说明在 offloading 场景中，草稿模型提出的候选 token 已不再像单卡常驻场景那样“每轮只确认 1 个 token”，而是能在大量步骤中一次性确认多个 token，从而显著减少目标模型真实参与解码的次数。

从机制上讲，offloading 场景的主要成本并不只来自目标模型本身的算子计算，还来自 CPU-GPU 之间的参数调度与搬运。此时，每减少一次目标模型真实解码，都意味着节省一轮显著的系统级数据移动成本。因此，当 verified token 数从常驻场景下的约 `1.0` 提升到 offloading 场景下的 `4.09` 时，speculative inference 的收益就得以直接体现为端到端加速。

## 5.4 与单卡常驻场景的对照分析

为了更清楚地说明 SpecInfer 收益的适用边界，本文将单卡常驻与单卡 offloading 两类场景进行对照。

首先，在机制指标上，常驻场景中的 `verified_mean` 长期约等于 `1.0`，说明 speculative 机制虽被成功触发，但并未实质减少目标模型真实解码步数；而 offloading 场景中的平均 `verified tokens` 达到 `4.09`，中位数为 `3`，大多数步骤都能够一次确认多个 token。

其次，在系统瓶颈上，常驻场景的目标模型已完整驻留 GPU，其主要瓶颈集中在模型本身逐 token 推理，因此 speculative 附加开销更容易压过收益；而 offloading 场景的关键瓶颈来自参数调度和搬运，此时只要目标模型真实解码步数下降，就能带来可观的系统级收益。

最后，在最终性能上，常驻场景的 speculative 路径稳定慢于 incremental baseline，而 offloading 场景则取得了 `3.84x - 3.93x` 的稳定 speedup。二者的差异表明，SpecInfer 是否“有用”不能脱离具体部署方式讨论，其收益本质上依赖于目标模型真实解码所携带的系统开销是否足够大，以及 speculative verification 能否显著提高平均每轮确认 token 数量。

## 5.5 与论文结论的关系

SpecInfer 论文在摘要中给出 offloading-based inference 可达 `2.6x - 3.5x` 的速度提升，而本文在单卡 `OPT-13B + OPT-125M` 实验中观测到 `3.85x - 3.93x` 的 speedup。虽然两者在具体实验平台、统计口径和运行参数上并非完全一致，但总体趋势是一致的：offloading 是 SpecInfer 最容易体现正向收益的部署场景之一。

需要说明的是，本文当前的 speedup 统计基于 benchmark 日志中的 `[Profile] latency(...)` 结果，本质上反映的是请求级或解码步骤级 latency 对比，而不是论文图表中严格逐 token 归一化后的全部指标。因此，本文在写作中更稳妥的表述方式应为：

> 在单卡 offloading 场景中，本文复现出了与论文主结论一致的显著正向收益，并在 `OPT-13B + OPT-125M` 配置下观察到约 `3.84x - 3.93x` 的端到端速度提升。

## 5.6 本章小结

本章对单卡 offloading 场景下的 SpecInfer 进行了重点评估。实验表明，在 `OPT-13B + OPT-125M` 的模型组合和 `batch size = 1/2/4` 条件下，SpecInfer 相对于 target-only offloading baseline 实现了约 `3.85x`、`3.84x` 和 `3.93x` 的稳定加速。进一步的日志统计显示，offloading speculative 路径的平均 verified token 数达到 `4.09`，显著高于单卡常驻场景中的 `1.0`。这说明在存在明显参数调度与搬运成本的部署环境中，SpecInfer 能够充分发挥 speculative verification 的优势，获得稳定的正向系统收益。

---

# 第6章 应用迁移：医疗问答与系统接入分析

## 6.1 应用迁移的目标与定位

虽然本文的主性能结论来自 offloading benchmark，但从应用落地角度看，推测推理技术的价值最终仍需通过具体问答系统、RAG 系统或 Agent 系统来体现。因此，本文引入了英文医疗问答 workload，并围绕 OpenAI 协议兼容接口进行了系统接入层面的设计与初步整理，目的是分析 SpecInfer 在应用迁移中的可行性，而非将医疗问答本身作为主性能 benchmark。

这种处理方式有两个好处。第一，它避免将“应用质量问题”与“底层推理收益问题”混为一谈，使性能结论仍然由标准 benchmark 支撑；第二，它能够从 workload 稳定性、输出截断风险、接口兼容性等角度，为后续系统接入提供工程经验。

## 6.2 英文医疗问答工作负载的稳定性

> [图6-1 插入位置]  
> 图6-1 英文医疗问答工作负载稳定性分布  
> 数据来源：`final_review_20260316_035130/english_len64/stability_summary_en/failure_summary.csv`  
> 对应图片：`论文图表/fig_6_1_english_stability.png`


本文构造了 50 条英文医疗问答数据，并在 `max-length = 64` 的稳定配置下，对 incremental baseline 和 speculative inference 进行了批量测试。结果见 [failure_summary.txt](/Users/Zhuanz/Documents/Typora/面经整理/面试/计算机/fresh/specinfer-ae/final_review_20260316_035130/english_len64/stability_summary_en/failure_summary.txt)。

实验结果表明，两种模式均表现出较高稳定性：

- `incr`：`46/50` 为 `ok`，`4/50` 为 `truncated`
- `spec`：`46/50` 为 `ok`，`4/50` 为 `truncated`

说明在受控长度预算下，SpecInfer 的推理链路已能够适应医疗问答类工作负载，且未出现大规模 runtime error、显存崩溃或明显乱码。这一点对于后续应用接入具有积极意义，因为它表明即使 speculative 模式未必在该工作负载上带来速度收益，其基本服务稳定性仍然是可接受的。

## 6.3 输出一致性代理与应用层解释

> [图6-2 插入位置]  
> 图6-2 英文医疗问答输出一致性代理分布  
> 数据来源：`final_review_20260316_035130/english_len64/acceptance_analysis_en/acceptance_metrics.csv`  
> 对应图片：`论文图表/fig_6_2_english_acceptance_proxy.png`


为了比较 speculative 与 baseline 在英文医疗问答上的输出相似性，本文进一步计算了输出一致性代理指标。结果见 [acceptance_summary.txt](/Users/Zhuanz/Documents/Typora/面经整理/面试/计算机/fresh/specinfer-ae/final_review_20260316_035130/english_len64/acceptance_analysis_en/acceptance_summary.txt)：

- `mean_acceptance_ratio = 0.991`
- `median_acceptance_ratio = 1.0`

这说明两种模式在该 workload 上的最终输出前缀整体保持高度一致。需要强调的是，这里的 acceptance ratio 本质上是输出一致性代理，而非论文中严格意义上的 speculative verification 接受率。因此，这一指标在本文中的作用主要是说明：在该应用工作负载上，即使 speculative 路径没有带来正向加速，其输出行为与 baseline 仍保持较高一致性，不会在服务层面引入明显的不稳定偏差。

## 6.4 系统接入层的意义

在系统设计层面，本文为后续的应用迁移预留了 OpenAI 协议兼容接口与 prompt 组织机制，使底层推理能力能够以 `/v1/chat/completions` 风格对外提供服务。这种封装方式的意义在于：

1. 便于与现有业务系统、Agent 框架和 RAG 链路对接；
2. 便于后续将 baseline 与 speculative 模式封装为统一服务接口；
3. 有助于将底层推理优化结果与上层问答系统组织方式解耦。

本文当前并未将系统接入结果作为主实验数据，而是将其视为一种应用迁移设计基础。这种处理方式能够保持论文主线聚焦于推理复现与性能评估，同时为未来扩展到医疗问答系统、Agent + RAG 系统提供清晰的实现路径。

## 6.5 本章小结

本章从应用迁移角度讨论了 SpecInfer 在英文医疗问答 workload 与系统接入背景下的表现。实验结果表明，在受控长度预算下，speculative 推理能够稳定适配英文医疗问答任务，但其在该工作负载上的主要价值体现在链路稳定性与输出一致性，而非速度提升。结合 OpenAI 兼容接口设计，本文认为 SpecInfer 更适合作为底层可替换推理引擎嵌入上层应用系统，而不宜直接将医疗问答 workload 作为其主性能实验场景。

---

# 第7章 复现难点、局限性与结论

## 7.1 复现难点总结

> [表7-1 可选插入位置]  
> 表7-1 SpecInfer 单卡复现中的主要问题与修复方法  
> 材料来源：`问题回顾.md`。


本文在复现 SpecInfer 过程中经历了较多工程问题，主要集中在以下几个方面。

第一，底层依赖复杂。SpecInfer 并非简单的 Python 推理脚本，而是建立在 FlexFlow、Legion、UCX、MPI 与 Rust 等多层依赖之上。任何一个环节缺失或版本不兼容，都可能导致构建失败或运行时错误。

第二，模型与缓存管理复杂。不同模型家族对 tokenizer 文件要求不同，FlexFlow 运行时读取的缓存路径也与 HuggingFace 默认缓存路径不同。若不显式同步 tokenizer 与配置文件，运行时极易出现“模型已下载但无法启动”的问题。

第三，受限单卡环境下的资源路径与运行参数高度敏感。AutoDL 默认系统盘容量较小，若不提前重定向缓存与临时目录，会在模型下载或权重转换过程中快速耗尽空间。与此同时，`zsize`、`max_seq_len`、batch size 等参数也必须结合容器上限和具体工作负载逐步收敛。

第四，长时间实验对中断恢复和结果记录提出了额外要求。若缺少 resumable 脚本、manifest 和结构化汇总，实验容易因 SSH 断线、脚本早停或输出目录混乱而丢失有效结果。因此，本文专门实现了可中断续跑、自动记录和最终导出的实验脚本体系。

这些复现难点表明，SpecInfer 的工程落地不能只依靠论文描述与官方脚本，还必须根据实际硬件平台、磁盘布局和运行时限制对实验流程进行系统化改造。

## 7.2 局限性分析

尽管本文已经完成了较完整的单卡复现与评估，但仍存在以下局限性。

首先，本文未复现论文中的多节点分布式结果。由于实验资源限制，本文无法覆盖原论文中面向 `OPT-30B`、`LLaMA-65B` 以及多节点 A10 集群的全部场景，因此结论主要针对单卡受限环境成立。

其次，本文在单卡常驻场景下未观察到正向 speedup。虽然这一本身构成了有价值的负结果，但它同时意味着：SpecInfer 并不能被简单视作“单卡大模型推理的通用加速方案”，其收益需要结合具体部署瓶颈进行理解。

第三，本文在医疗问答工作负载上的 acceptance 指标主要是输出一致性代理，而非论文严格意义上的真实接受率。虽然这足以支撑应用迁移层面的稳定性讨论，但不能直接替代 speculative verification 的完整机制统计。

第四，本文对于系统接入层的验证仍以接口封装、工作负载设计和原型支持为主，尚未在完整的业务端框架中进行大规模、长周期在线评估。因此，本文关于 Agent/RAG 场景的讨论更多是迁移性分析，而非成熟产品级验证。

## 7.3 主要结论

围绕本文提出的三个研究问题，可以得出以下结论。

第一，SpecInfer 的核心推测推理机制可以在单卡 A100 环境中成功复现。本文完成了 FlexFlow/Legion 构建、模型准备、推理运行、结果采集与导出等全流程实现，说明 SpecInfer 在受限单卡条件下仍具有可复现实验基础。

第二，SpecInfer 的收益高度依赖具体部署瓶颈。在单卡常驻 `LLaMA-7B` 场景下，SpecInfer 虽然能够稳定运行，但平均每轮确认 token 数量长期约等于 `1`，未能带来端到端加速；而在单卡 offloading `OPT-13B + OPT-125M` 场景下，平均 verified token 数提升到 `4.09`，并进一步转化为约 `3.84x - 3.93x` 的稳定 speedup。

第三，英文医疗问答 workload 适合作为应用迁移与稳定性分析场景，而不是主性能 benchmark。在受控长度预算下，SpecInfer 在该工作负载上具有较高稳定性和输出一致性，但并未显著改善推理速度。因此，应用层工作负载更适合用于讨论服务可用性与迁移边界，而核心性能结论仍应以标准 benchmark 和 offloading 实验为基础。

## 7.4 未来工作

未来工作可以沿以下方向展开。

第一，进一步扩展至论文中的分布式多 GPU 或多节点场景，验证 SpecInfer 在 activation transfer 开销更重的部署区间是否能够复现更完整的论文结果。

第二，引入更系统的 per-token latency 统计口径，使本文结果能够与论文图表进行更严格的一一对照。

第三，在医疗问答或 Agent + RAG 系统中完成更完整的接口接入和长周期评估，将底层 speculative inference 的系统收益进一步映射到上层业务体验与服务吞吐。

第四，探索更适合中文问答或专业领域问答的草稿模型选择与 prompt 组织方式，以提升在应用场景下的 verified token 数量和稳定性。

## 7.5 全文总结

本文围绕 SpecInfer 在受限单卡环境下的复现与评估展开研究，构建了一套可运行、可恢复、可导出的实验体系，并在单卡常驻与单卡 offloading 两类场景下对其性能表现进行了系统比较。实验表明，SpecInfer 并非在所有单卡场景中都能带来收益：在 `LLaMA-7B` 常驻短回答场景中，speculative 路径虽然可运行，但未体现端到端加速；而在 `OPT-13B` 的 offloading 场景中，SpecInfer 则取得了稳定显著的正向收益。

因此，本文的核心结论并不是“SpecInfer 在单卡环境下普遍有效”，而是：**SpecInfer 的收益与系统瓶颈和 verified token 分布密切相关，在存在明显参数调度与搬运开销的 offloading 环境下，SpecInfer 能够充分体现其设计优势。** 这一结论既回应了论文中的主张，也为后续在受限资源环境下部署推测推理系统提供了更具工程现实性的参考。

---

# 第1章 绪论

## 1.1 研究背景

近年来，大型语言模型（Large Language Models, LLMs）在通用问答、代码生成、文本摘要、专业知识问答等任务中表现出显著优势，逐渐成为智能问答系统、检索增强生成系统以及多智能体协同系统中的核心推理引擎。然而，随着模型参数规模的不断增加，大模型推理阶段的时间成本和硬件资源开销迅速上升，模型部署与在线服务效率问题日益突出。尤其是在资源受限的部署环境中，如何在保证生成质量基本不变的前提下提升推理速度，已经成为大模型工程落地中的关键问题。

针对上述问题，推测解码（Speculative Decoding）成为近年来广受关注的推理加速方向。该类方法通常引入一个较小的草稿模型（draft model）与一个较大的目标模型（target model），先由草稿模型生成候选 token，再由目标模型进行验证，从而减少大模型逐 token 真实解码的次数。早期工作已经表明，在草稿模型与目标模型分布较为接近时，推测解码能够在保持输出质量的同时显著降低生成延迟。进一步的系统级研究则开始关注：在真实在线服务环境下，如何让推测解码不仅在理论上成立，而且在多种部署瓶颈下体现可观的端到端收益。

SpecInfer 是这一方向上的代表性系统。与传统的线性 speculative decoding 不同，SpecInfer 进一步提出了 token tree 结构，并通过 tree-based verification 并行验证多个候选前缀，从而在系统层面放大推测解码的收益。SpecInfer 论文指出，该机制在两类场景中尤为有效：一类是分布式大模型推理，另一类是基于 offloading 的大模型推理。前者主要受到跨设备激活传输与同步开销限制，后者则主要受到 CPU-GPU 参数调度和搬运开销影响。SpecInfer 的核心意义在于：当目标模型推理瓶颈不再仅仅来自算子计算，而更多来自系统级数据移动时，减少目标模型真实解码步数会带来更显著的收益。

尽管 SpecInfer 的论文结果较为亮眼，但其工程复现并不简单。一方面，原始实现依赖 FlexFlow、Legion、UCX、MPI、Rust 等多层底层依赖，部署环境复杂；另一方面，论文中的最强结果主要基于分布式多卡和 offloading 场景，而普通单卡用户往往只能在资源受限环境中进行局部复现。因此，一个具有现实意义的问题是：在单卡受限环境下，SpecInfer 的哪些结论能够稳定复现，哪些结论不能直接迁移？进一步地，SpecInfer 在面向应用落地的场景中，例如医疗问答或 Agent + RAG 系统接入时，又表现出怎样的收益边界与工程限制？

基于此，本文以 SpecInfer 为研究对象，围绕“受限单卡环境下的复现与评估”展开工作。本文并不试图宣称完整复现论文中全部分布式结论，而是聚焦于两个更具工程实际意义的问题：其一，SpecInfer 的核心推测推理机制能否在单卡 A100 环境下稳定复现；其二，在单卡常驻推理与单卡 offloading 推理两类不同系统瓶颈下，SpecInfer 是否能展现一致的加速效果。通过对这两个问题的对比研究，本文进一步分析推测推理收益与验证 token 数、部署方式及任务形态之间的关系，并讨论其在医疗问答与系统接入背景下的应用可行性。

## 1.2 研究意义

本文的研究意义主要体现在以下三个方面。

第一，本文对 SpecInfer 进行了工程级复现与环境收敛。相较于直接使用论文中的理论描述，工程复现更能揭示该系统在真实部署中的依赖要求、缓存路径设计、模型下载方式、底层编译链、以及中断恢复与结果记录机制等问题。通过在 AutoDL 单卡 A100 环境下完成完整复现，本文为后续研究者在类似受限环境中开展相关实验提供了可借鉴的工程路径。

第二，本文对 SpecInfer 的适用边界进行了实证分析。现有论文中对 SpecInfer 的正向结果多建立在分布式或 offloading 场景下，而普通用户更常接触的是单卡常驻推理环境。本文通过对单卡常驻 `LLaMA-7B + LLaMA-68M/160M` 与单卡 offloading `OPT-13B + OPT-125M` 两条路线的系统比较，说明 SpecInfer 的收益并非无条件成立，而是与系统瓶颈、验证 token 数量以及任务形态高度相关。

第三，本文将推测推理的系统级实验与应用层落地背景结合起来进行讨论。当前大模型在医疗问答、RAG 和 Agent 系统中的应用越来越广泛，但推理效率瓶颈依然制约其实用性。本文虽然不把医疗问答作为主性能实验场景，但通过引入英文医疗问答 workload 与系统接口设计分析，展示了 SpecInfer 在实际系统迁移中的潜在价值和局限，为后续将推测推理接入专业领域问答系统提供了参考。

## 1.3 研究问题

围绕上述背景，本文重点回答以下三个研究问题：

1. SpecInfer 的核心推测推理机制能否在单卡 A100 环境下稳定复现？
2. 在单卡常驻推理与单卡 offloading 推理两类场景下，SpecInfer 的收益是否一致？若不一致，造成差异的主要机制是什么？
3. 在英文医疗问答 workload 与面向 OpenAI 协议的系统接入背景下，SpecInfer 的实验结果能够提供哪些工程启示？

## 1.4 研究内容

为回答上述问题，本文开展了以下研究工作。

首先，本文完成了 SpecInfer AE 的工程复现，包括 FlexFlow/Legion 环境编译、UCX 与 MPI 依赖安装、HuggingFace 与 FlexFlow 缓存重定向、模型下载与 tokenizer 同步、自动实验记录与结果导出等。通过这些工作，构建了一套适用于单卡 A100 环境的可重复实验框架。

其次，本文设计并实现了两条实验路线。第一条路线为单卡常驻推理实验，以 `LLaMA-7B` 为目标模型，`LLaMA-68M` 或 `LLaMA-160M` 为草稿模型，对比 `incremental decoding`、`sequence-based speculative inference` 和 `tree-based speculative inference` 三种模式。第二条路线为单卡 offloading 推理实验，以 `OPT-13B` 为目标模型、`OPT-125M` 为草稿模型，对比 target-only offloading baseline 与 speculative offloading 的端到端性能差异。

再次，本文构建了英文短 prompt、英文医疗问答 prompt 以及官方风格 continuation prompt 等多类 workload，对稳定性、接受率代理指标、verified token 分布和端到端延迟进行测量，分析不同 workload 对 SpecInfer 收益的影响。

最后，本文在实验结果基础上对 SpecInfer 的适用边界进行总结，指出其在常驻单卡短回答场景中的限制，并给出其在 offloading 场景下获得稳定正向收益的证据与解释。

## 1.5 主要贡献

本文的主要贡献可以概括为以下三点：

1. 在 AutoDL 单卡 A100 环境中完成了 SpecInfer 的工程复现，建立了一套可中断续跑、可自动记录、可导出结果的单卡实验流程。
2. 通过对比单卡常驻与单卡 offloading 两类部署方式，揭示了 SpecInfer 的收益高度依赖系统瓶颈与验证 token 数量：常驻场景中机制可运行但未体现速度收益，而 offloading 场景中则获得了稳定的正向加速。
3. 将推测推理实验与应用迁移背景结合起来，构建了医疗问答与 OpenAI 兼容接口接入的实验支撑材料，为后续在专业领域问答系统中部署推测推理提供了工程经验。

## 1.6 论文结构安排

全文结构安排如下：

- 第 1 章介绍研究背景、研究问题、研究内容与本文贡献。
- 第 2 章梳理推测解码、SpecInfer、RAG 与医疗问答、多智能体协同等相关工作。
- 第 3 章介绍 SpecInfer 的机制概念、实验平台、系统实现与复现方法。
- 第 4 章分析单卡常驻场景下的实验结果，说明其未观察到明显加速的原因。
- 第 5 章分析单卡 offloading 场景下的实验结果，给出 baseline 与 speculative 的速度对比及机制解释。
- 第 6 章讨论英文医疗问答 workload 与系统接入背景下的应用迁移问题。
- 第 7 章总结全文工作，讨论复现难点、局限性与未来工作。

---

# 第3章 系统设计与复现方法

## 3.1 SpecInfer 机制概述

> [图3-1 插入位置]  
> 图3-1 SpecInfer 三种推理模式示意图  
> 注：图中对比 incremental decoding、sequence-based speculative inference 与 tree-based speculative inference 的验证路径差异，并突出 verified token 对加速效果的决定作用。


SpecInfer 的基本思想是在目标模型真实解码之前，引入一个较小的草稿模型对未来若干 token 进行预生成，然后由目标模型对这些候选 token 进行并行验证，从而尽可能减少目标模型逐 token 解码的次数。与传统仅沿单一路径进行 speculative decoding 的方法不同，SpecInfer 将候选 token 组织成 token tree，并借助 tree-based verification 在一次验证中覆盖更多可能前缀，以提高平均每步可确认的 token 数量。

从运行模式上看，本文涉及三种主要推理方式：

1. **Incremental Decoding**  
   即目标模型逐 token 顺序生成，是本文所有 speculative 实验的 baseline。

2. **Sequence-based Speculative Inference**  
   该模式可视为 tree width 为 `1` 的特例，即沿单一路径由草稿模型提出候选，再由目标模型验证。

3. **Tree-based Speculative Inference**  
   该模式是 SpecInfer 的核心创新，通过将多个候选 token 扩展为树结构，使目标模型在一次验证中尽可能确认更多 token。

对于 SpecInfer 而言，决定其收益高低的关键机制指标并不是“是否使用了 speculative inference”，而是**每轮验证究竟确认了多少 token**。如果平均每步只能确认 1 个 token，那么 speculative 路径的额外草稿生成、验证调度和系统级 bookkeeping 开销将难以被摊薄；反之，如果平均每步能够确认多个 token，则目标模型真实解码步数减少所节省的成本将逐渐超过 speculative 路径的附加开销。因此，本文在实验分析中重点关注 verified token 的统计分布，并将其作为解释加速或未加速现象的核心依据。

## 3.2 总体系统结构

> [图3-2 插入位置]  
> 图3-2 本文实验与应用迁移总体框架  
> 注：上层为 OpenAI 兼容接口、医疗问答与 Agent/RAG 应用迁移层，下层为模型下载、FlexFlow 构建、单卡常驻实验、offloading 实验、日志汇总与结果导出等复现层。


本文的系统结构可以概括为“实验复现层”与“应用迁移层”两个层次。

在实验复现层，本文以 SpecInfer 官方 AE 代码仓库为基础，构建了单卡可运行的实验脚本集合，覆盖模型下载、环境配置、常驻推理 benchmark、offloading benchmark、日志收集、结果汇总与实验导出等环节。该层关注的是底层推理系统是否能稳定运行，以及不同推理模式下的性能差异。

在应用迁移层，本文进一步构建了 OpenAI 协议兼容的接口封装与医疗问答 workload，以验证推测推理在实际系统接入背景下的可用性。该层不以追求最优性能为目标，而是更关注接口兼容性、请求组织、稳定性以及应用落地边界。

因此，本文整体并不是将医疗问答系统作为主性能实验对象，而是将其视为一个上层应用背景；真正承载性能结论的是底层推理复现实验，尤其是后续的 offloading 路线。

## 3.3 实验平台与硬件环境

> [表3-1 插入位置]  
> 表3-1 实验环境与关键参数配置  
> 建议字段：GPU 型号、CUDA 版本、核心依赖、缓存根目录、常驻实验模型组合、offloading 实验模型组合、batch size、max sequence length、ZSIZE/FSIZE、manifest 记录方式。


本文实验主要在 AutoDL 提供的单卡 A100 环境中完成，GPU 型号为 `NVIDIA A100-PCIE-40GB`。与原始论文中涉及的多节点、多 GPU 分布式配置不同，本文聚焦于单卡受限环境，目的是考察 SpecInfer 在更常见、更低成本部署条件下的可复现性与实际收益。

在软件环境方面，本文复现过程中涉及如下关键组件：

- `FlexFlow`：作为 SpecInfer 的运行时基础框架；
- `Legion`：负责底层任务图执行与调度；
- `UCX` 与 `MPI`：用于支持 FlexFlow/Legion 所需的底层通信依赖；
- `Rust`：用于编译 `tokenizers-c` 等第三方组件；
- `HuggingFace`：用于模型与 tokenizer 下载；
- `Python + shell 脚本`：用于组织实验流程、日志提取和结果汇总。

考虑到 AutoDL 默认系统盘容量有限，本文在实验一开始即将 HuggingFace 缓存、FlexFlow 缓存和临时文件目录统一重定向至数据盘，并通过环境变量固定模型缓存与中间文件路径，以避免系统盘空间不足导致下载或转换阶段中断。类似地，针对 tokenizer 文件缺失、Legion Python 绑定缺失、UCX/mpi 依赖不完整等问题，本文也通过补丁与脚本化方式进行了长期修复，而非一次性手工处理。

## 3.4 复现流程设计

> [图3-3 插入位置]  
> 图3-3 单卡 SpecInfer 复现实验流程图  
> 注：流程包含环境构建、模型准备、常驻实验、offloading 实验、日志汇总与结果导出。


为了提高实验稳定性与可重复性，本文将 SpecInfer 的复现过程拆解为以下几个阶段：

### 3.4.1 环境构建阶段

该阶段主要完成：

- 项目源码与子模块拉取；
- UCX、MPI、Rust 等底层依赖安装；
- FlexFlow 与 SpecInfer 编译；
- Python 运行环境与 Legion/FlexFlow 绑定修复；
- 国内网络镜像与缓存路径设置。

在这个阶段中，最重要的设计原则是“**先修环境，再跑模型，再做实验**”。与官方 AE 直接通过总脚本执行不同，本文将构建流程显式拆分，以便在 AutoDL 环境下逐步定位错误来源，并将修复逻辑固化为脚本。

### 3.4.2 模型准备阶段

该阶段完成不同模型的下载、转换与 tokenizer 同步，主要涉及两组模型：

1. **常驻推理模型组**
   - 目标模型：`huggyllama/llama-7b`
   - 草稿模型：`JackFram/llama-68m` 与 `JackFram/llama-160m`

2. **offloading 推理模型组**
   - 目标模型：`facebook/opt-13b`
   - 草稿模型：`facebook/opt-125m`

由于不同模型家族对 tokenizer 文件的要求不同，本文在下载脚本中加入了专门的同步逻辑。对于 LLaMA 系列，重点保证 `tokenizer.model` 正确落入 FlexFlow 缓存路径；对于 OPT 系列，则显式同步 `vocab.json`、`merges.txt`、`tokenizer.json`、`tokenizer_config.json` 等文件，避免推理阶段出现“模型可见但 tokenizer 不完整”的问题。

### 3.4.3 实验执行阶段

本文将实验划分为两条路线。

**第一条路线：单卡常驻推理实验**

该路线主要复现官方 `server_gpu_experiments.sh` 中的单卡部分。固定目标模型为 `LLaMA-7B`，草稿模型为 `68M` 或 `160M`，分别运行：

- `incremental decoding`
- `sequence-based speculative inference`
- `tree-based speculative inference`

该路线的目的是验证：在目标模型已经常驻 GPU 的前提下，SpecInfer 的 speculative 机制能否带来显著速度收益。

**第二条路线：单卡 offloading 推理实验**

该路线主要复现官方 `offloading_experiments.sh` 的单卡思路。固定目标模型为 `OPT-13B`，草稿模型为 `OPT-125M`，分别运行：

- `target-only offloading baseline`
- `sequence-based speculative offloading`

在这一场景中，目标模型部分参数需要在 CPU 与 GPU 之间调度，因此理论上更有机会通过减少真实解码步数获得系统级收益。本文最终的主性能结果也来自这一实验路线。

### 3.4.4 结果汇总阶段

为了避免仅依赖屏幕输出进行人工统计，本文额外实现了多类后处理脚本，用于：

- 提取 `[Profile] latency(...)`；
- 统计 batch 维度的 mean / p50 latency；
- 统计 verified token 分布；
- 比较 baseline 与 speculative 的 speedup；
- 生成 acceptance proxy、稳定性摘要和实验 manifest。

此外，本文还实现了实验导出脚本，将最终实验结果、日志、prompt、manifest 和汇总 CSV 打包导出，以便后续论文写作与结果回溯。

## 3.5 数据与 Prompt 设计

本文在不同实验目标下使用了不同类型的 prompt。

首先，为了尽量贴近官方实验，本文在常驻 benchmark 中采用了与 `chatgpt_1.json`、`chatgpt_2.json`、`chatgpt_4.json` 相同风格的英文开放式 continuation prompt，以避免引入过强的任务特化偏差。

其次，为了考察应用迁移背景下的稳定性，本文构造了 50 条英文医疗问答数据，并将其用于英文 `len64` 实验。这部分实验不以获得最强 speedup 为目标，而主要用于观察：

- 请求是否能稳定完成；
- 输出是否出现截断或明显乱码；
- speculative 与 baseline 输出的一致性代理指标。

最后，在 offloading 场景中，本文采用官方 `chatgpt_offloading.json` 作为 workload，以便尽可能与论文中的设置保持一致，并在 `batch=1/2/4` 条件下对 baseline 与 speculative 进行公平比较。

## 3.6 实验记录与可复现性设计

为提高实验的可追踪性，本文在 benchmark 脚本中引入了 manifest 记录机制。每次实验启动时，系统会自动记录：

- 时间戳；
- git 分支与 commit；
- 关键环境变量；
- GPU 信息；
- 磁盘与内存状态；
- 模型组合；
- prompt 文件与 batch 参数。

同时，实验输出目录中会保留：

- `.out`：完整运行日志；
- `.txt`：模型生成内容；
- `summary.csv`：结构化汇总结果；
- `manifest.txt/json`：实验元信息；

这种设计使得本文的实验不仅能够“跑通”，还能够在后续论文写作阶段清晰回答以下问题：某一条实验到底使用了什么参数、在哪个 commit 上运行、其 latency 统计来自哪里、以及对应的日志证据是否仍然可追溯。

## 3.7 本章小结

本章围绕本文的复现目标与系统实现给出了整体设计。首先介绍了 SpecInfer 的三类核心推理模式，并指出 verified token 是理解其收益的关键机制指标。随后，本文说明了单卡 A100 环境下的实验平台、复现流程、模型准备方式、两条实验路线与数据记录设计。通过上述系统化设计，本文构建了一个可在受限单卡环境中稳定执行的 SpecInfer 复现实验框架，为后续实验结果分析提供了基础。
