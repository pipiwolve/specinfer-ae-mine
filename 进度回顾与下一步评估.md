# SpecInfer 毕设进度回顾与下一步评估

## 1. 当前整体结论

当前论文已经具备可交付条件，且主线已经清晰：

- **主正结果**：单卡 offloading 路线已经跑出稳定正向 speedup，可作为正文核心实验结果。
- **主负结果**：单卡常驻 resident 路线已经多次验证未复现正向加速，可作为边界分析与失败实验。
- **应用迁移结果**：英文 `len64` 与医疗问答链路已经证明系统可以跑通，但不适合作为主性能结论。

因此，论文现在最合理的命题不是“完整复现 SpecInfer 所有结果”，而是：

> 在单卡资源约束下复现并评估 SpecInfer，发现其在 offloading 场景下能够获得显著加速，而在常驻短回答场景下未观察到收益。

---

## 2. 已完成事项

### 2.1 环境与工程侧

已完成：

- AutoDL 单卡环境搭建完成
- FlexFlow / SpecInfer 编译完成
- HuggingFace / FlexFlow 缓存路径已迁移到 `/root/autodl-tmp`
- `LLaMA-7B + 68M/160M`、`OPT-13B + OPT-125M` 等关键模型下载链路已打通
- 已补齐：
  - tokenizer 同步
  - Legion Python binding
  - mainland mirror
  - manifest 自动记录
  - 可续跑 benchmark 脚本

对应脚本体系已基本成型：

- resident benchmark
- offloading benchmark
- baseline/spec 对比
- batch 结果汇总
- 图表自动生成

### 2.2 常驻 resident 路线

已完成：

- 单条 `incr_decoding` smoke test 跑通
- 单条 `spec_infer` smoke test 跑通
- 英文 `len64` 批量实验跑通
- 官方风格 prompt 测试跑通
- `68m`、`160m`、`round 对齐版` 三轮 resident benchmark 跑通

已确认结论：

- 当前 A100 环境下，resident 路线 **未观察到加速**
- 关键原因是 `verified_mean ≈ 1`
- `sequence/tree` 延迟均高于 `incr`

### 2.3 Offloading 路线

已完成：

- `OPT-13B + OPT-125M` offloading speculative benchmark 跑通
- `OPT-13B` offloading baseline 跑通
- baseline/spec 对比汇总完成

已确认结论：

- `bs=1`: speedup 约 `3.85x`
- `bs=2`: speedup 约 `3.84x`
- `bs=4`: speedup 约 `3.93x`

同时，机制证据也已具备：

- verified token 平均值显著大于 `1`
- 说明 speculative verification 在 offloading 路线下确实产生了实际收益

### 2.4 论文材料

已完成：

- 论文故事线梳理
- 第 1 章与第 3 章初稿
- 摘要、第 2、4、5、6、7 章初稿
- 完整整合稿
- 图表插入版
- 最终排版版 Markdown
- 绘图脚本与论文图表

---

## 3. 已经成立的论文结论

以下结论已经可以安全写入正文：

### 3.1 可以成立

1. 本文成功复现了 SpecInfer 的核心工程链路。
2. 在单卡 offloading 场景下，SpecInfer 获得了稳定且显著的正向加速。
3. 在单卡常驻短回答场景下，虽然 speculative 机制能够运行，但未观察到端到端加速。
4. SpecInfer 的收益与 verified token 数量密切相关。
5. Offloading 场景比单卡常驻场景更符合论文中 SpecInfer 的优势区间。

### 3.2 不应直接写

1. “本文完整复现了 SpecInfer 全部论文结果”
2. “SpecInfer 在单卡常驻场景下显著加速”
3. “SpecInfer 已在医疗问答系统中带来显著性能提升”
4. “当前默认 API 服务已经运行在论文主正结果对应的 offloading 形态上”

---

## 4. 当前不建议继续投入的方向

以下方向现在不建议继续投入大量时间：

### 4.1 resident 正结果继续追

原因：

- 已做过多轮 resident 验证
- `160m` 与 `round 对齐版` 仍然失败
- 继续调 resident 参数的收益很低

建议：

- resident 到此为止
- 保留为负结果与边界分析

### 4.2 立即切 Qwen 适配

原因：

- 当前框架尚不直接支持 `Qwen2ForCausalLM / Qwen3ForCausalLM`
- 需要改模型注册、权重转换、tokenizer 同步
- 与当前论文收口关系不大

建议：

- 只保留为“未来工作”或后续探索方向

### 4.3 再开新 A100 / 4090 大规模补 resident 实验

原因：

- 对当前论文主线帮助有限
- 会明显拉长收稿时间
- 成功率并不高

建议：

- 暂停该讨论
- 当前以现有结果收口

---

## 5. 当前论文最强证据链

论文当前最强的逻辑链应该是：

1. **机制复现**  
   已成功跑通 `incr / sequence / tree` 与 offloading speculative。

2. **负结果解释**  
   resident 路线中 `verified_mean ≈ 1`，因此 speculative overhead 无法被摊薄。

3. **正结果证明**  
   offloading 路线中 verified token 明显大于 `1`，并取得 `3.84x–3.93x` speedup。

4. **应用迁移边界**  
   医疗问答与英文短回答任务说明：系统可运行，但收益依赖 workload 与部署形态。

---

## 6. 当前还缺什么

严格来说，论文核心实验已经够了。现在缺的更多是**写作与答辩层面的收口**。

### 6.1 必须补完

1. 最终论文正文排版
2. 图表插入与编号统一
3. 参考文献核对
4. 摘要、中英文关键词统一
5. 答辩讲稿与答辩问题准备

### 6.2 建议补完

1. 一张系统总体流程图
2. 一张 SpecInfer 三种模式对比示意图
3. 一张“resident 无收益 vs offloading 有收益”的机制解释图
4. 若保留系统演示，则补 1 张 API 调用或系统界面截图

### 6.3 可选补充

1. 将 old round / legacy opt13b / tree width 实验整理成附录
2. 追加一段“未来工作：Qwen 适配”讨论

---

## 7. 下一步推荐顺序

### 阶段 A：正文定稿

按顺序完成：

1. 以 `论文终稿排版版.md` 为主稿
2. 插入正文图表
3. 统一 resident / offloading / 医疗 / API 的表述口径
4. 修正参考文献与术语

### 阶段 B：附录整理

可将以下内容放附录：

- old round resident 正结果
- legacy `OPT-13B` 全量 offloading 结果
- tree width 对比
- 环境踩坑与复现问题回顾

### 阶段 C：答辩准备

优先准备：

1. 为什么 resident 没有正结果
2. 为什么 offloading 有正结果
3. 为什么 old round 不作为正文主结论
4. 当前 API 默认为什么不是 offloading
5. Qwen 为什么暂不纳入当前主实验

---

## 8. 当前进度评估

### 8.1 完成度

按毕业论文实际交付口径估计：

- 实验完成度：**85%–90%**
- 正文写作完成度：**70%–80%**
- 图表与附录完成度：**65%–75%**
- 答辩准备完成度：**40%–50%**

### 8.2 风险等级

- 实验风险：**低**
  - 主结果已具备
- 论文风险：**中低**
  - 只要口径不飘，论文是成立的
- 答辩风险：**中**
  - 主要风险来自叙事不够收敛，以及 old round / resident / offloading 的关系解释不清

---

## 9. 当前最合理的总策略

一句话总结：

> 停止继续追 resident 正结果，把论文主线彻底收敛到 “单卡受限条件下的 SpecInfer 复现与评估，其中 offloading 路线取得显著正向加速，resident 路线揭示其适用边界”。

这条策略最稳、最真实，也最符合你现在已经拿到的数据。
