# 进度追踪｜7天冲刺 To-do (1)

<aside>
⚠️

**规则：每天只要完成“验收产出物”，当天就算过关。其他一律延后，不许加戏。**

</aside>

### 总览（Day 1 → Day 7）

- [ ]  Day 1：业务端跑通（本地 yu-ai-agent + GLM-4 + RAG）
- [ ]  Day 2：API 网关伪装（FastAPI 模拟 OpenAI 协议 + 公网连通）
- [ ]  Day 3：核心推理接入（Transformers 推测解码接入 /v1/chat/completions）
- [ ]  Day 4：医疗场景注入与调优（50 条 QA + 工程稳定性）
- [ ]  Day 5：微观实验（草稿通过率/接受率 + 图表）
- [ ]  Day 6：宏观实验（系统加速比 Latency 对比 + 图表）
- [ ]  Day 7：论文故事线重构（写作 + 失败包装成结论）

---

### Day 1｜业务端跑通（本地）

**阶段目标**：本地 `liyupi/yu-ai-agent` 只保留对话 + RAG，稳定跑通。

**Action Items**

- [ ]  拉起后端（Spring Boot 3）与前端，确保能正常登录/进入对话页
- [ ]  接入免费/低成本商用 API（如智谱 GLM-4），跑通纯文本对话
- [ ]  跑通“本地文档上传 → 向量化 → 检索 → 拼接上下文 → 回答”的完整 RAG 流程

**Deliverable（验收产出物）**

- [ ]  录屏或截图：一次成功的对话
- [ ]  录屏或截图：一次成功的 RAG（上传文档 + 命中检索 + 回答中出现引用内容）

**最高优风险排查（最容易卡死点）**

- [ ]  工程依赖/版本冲突导致无法启动：先用最小配置启动，再补功能
- [ ]  RAG 链路失败（向量库/Embedding 配置不对）：先用默认示例数据验证，再换文档

---

### Day 2｜API 网关伪装（AutoDL）

**阶段目标**：FastAPI 对外暴露标准 OpenAI `/v1/chat/completions`，先返回静态 JSON；业务端可切 Base URL。

**Action Items**

- [ ]  在 AutoDL 建 FastAPI 最小服务：`/v1/chat/completions` 接收请求并返回固定 JSON
- [ ]  打通公网端口映射（能从本地 curl 访问）
- [ ]  本地 Java 端把 Base URL 切到 AutoDL 地址，验证连通与返回解析

**Deliverable（验收产出物）**

- [ ]  一条可复现的 curl 命令 + 返回内容（200 OK）
- [ ]  Java 端对话请求能命中 AutoDL 并显示“静态回答”

**最高优风险排查**

- [ ]  OpenAI 协议字段不全导致 Java 端解析崩：严格对齐 `id/object/created/model/choices/message/usage`
- [ ]  公网不通：先在服务器本地 curl 自己，再做端口暴露

---

### Day 3｜核心推理接入（推测解码 + 全链路）

**阶段目标**：FastAPI 真实调用 Transformers 推测解码（LLaMA-7B + LLaMA-68M），并按 OpenAI 格式返回。

**Action Items**

- [ ]  接入 Transformers 推测解码代码到 `/v1/chat/completions`
- [ ]  做好 JSON 解析：拼接 RAG 上下文 + 用户问题，形成最终 prompt
- [ ]  返回格式对齐 OpenAI，确保端云全链路稳定跑通

**Deliverable（验收产出物）**

- [ ]  本地前端发起一次 RAG 问答，结果由 AutoDL 的 LLaMA 推理返回（非静态）
- [ ]  保存一份请求 JSON 与响应 JSON（用于论文附录/复现）

**最高优风险排查**

- [ ]  显存炸（模型加载 OOM）：先 4bit/8bit 量化或降 max

tokens，必要时先只跑 68M 验证链路

- [ ]  prompt 过长：必须加上下文截断策略（优先保留检索片段 + 问题）

---

### Day 4｜医疗场景注入与调优（稳定性）

**阶段目标**：用 50 条医疗 QA 做稳定性与工程问题兜底。

**Action Items**

- [ ]  找到并整理 50 条医疗问答（含问题与参考答案或来源）
- [ ]  批量测试：记录失败类型（CORS、超长、超时、显存、输出乱码等）
- [ ]  工程修复：CORS、max tokens、stream 处理（Java 端关闭或 Python 端模拟 chunk）

**Deliverable（验收产出物）**

- [ ]  一份 50 条 QA 的文件（json/csv/markdown 均可）
- [ ]  一张“问题类型 → 失败率/修复状态”的小表

**最高优风险排查**

- [ ]  前端跨域：FastAPI 一开始就加 CORS 中间件
- [ ]  上下文超长导致爆显存：硬性限制输入长度与 `max_new_tokens`

---

### Day 5｜微观实验：草稿通过率（接受率）

**阶段目标**：单独脚本测“68M 草稿在医疗 prompt 下的接受率”，产出图。

**Action Items**

- [ ]  写独立 Python 脚本：固定一组医疗 prompts，记录推测解码接受/拒绝统计
- [ ]  计算指标：平均接受率、p50/p95（至少给均值 + 方差/分位数其一）
- [ ]  画图：接受率分布或不同 prompt 类别对比

**Deliverable（验收产出物）**

- [ ]  可运行脚本 + 一份输出结果（csv/json）
- [ ]  1 张图（png）+ 3 句结论（能写进论文）

**最高优风险排查**

- [ ]  指标采集不到：先在日志里打印每步接受/拒绝，再整理成结构化数据

---

### Day 6｜宏观实验：系统加速比（端到端 Latency）

**阶段目标**：同样的请求集，比较开启/关闭推测解码的端到端延迟，得出加速比。

**Action Items**

- [ ]  在 FastAPI 内部打点：请求进入、token 生成、请求结束（至少能得端到端）
- [ ]  做 A/B：Speculative On vs Off，跑同一批 prompts
- [ ]  计算加速比并画柱状图（均值或 p50/p95）

**Deliverable（验收产出物）**

- [ ]  一份延迟日志（csv）
- [ ]  对比柱状图（png）+ 明确写出加速比（例如 2.1×）

**最高优风险排查**

- [ ]  网络波动污染结果：同一时段连跑多次，取中位数；或尽量在服务器侧压测

---

### Day 7｜论文故事线重构（写作与包装）

**阶段目标**：把“多模型协作”写成上下两层协作：上层 Agent+RAG，多智能体；下层目标/草稿模型推测协作。把 C++/Offloading 失败写成资源受限下的结论。

**Action Items**

- [ ]  重写摘要与引言：问题动机、系统架构、贡献点（3 条以内）
- [ ]  写实验章节：微观接受率 + 宏观加速比（图必须嵌入）
- [ ]  写“失败实验结论”：OOM 现象、原因分析、得到的约束与启示

**Deliverable（验收产出物）**

- [ ]  论文初稿 v1（至少：摘要/架构/实验/结论齐全）
- [ ]  所有图表插入并编号（可交付）

**最高优风险排查**

- [ ]  写不下去：先按“图 → 结论 → 文字解释”顺序填空式完成

---

### 每日 Check-in（强制）

- [ ]  今天的 **Deliverable** 是否完成？没完成就写“卡点一句话 + 下一步动作一句话”。
- [ ]  今天是否新增了需求？如果有，必须删掉同等工作量的其他项（保持总量不变）。
- [ ]  明天 Day X 的第一件事是什么？写成一句可执行命令（例如：`curl ...` 或 `python ...`）。