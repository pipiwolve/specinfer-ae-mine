# SpecInfer AE 实验踩坑与避坑指南 (AutoDL 单机版)

在整个复现顶会推荐流程（`download_models.sh`、`install_specinfer.sh`、`basic_test.sh`）的过程中，我们遭遇了系统架构、底层算子、C++ 依赖封装以及国内网络和挂载硬盘等多方面的连环坑。

为了下一次能够“一键如履平地”，特整理这份**避坑大全**：

---

### 1. 硬件架构兼容性：显卡与推测解码算子的深度冲突
- **现象**：在 RTX 4090 上执行官方流程，在加载或生成阶段触发 `Segmentation fault (core dumped)`，且没有任何 Python 或 C++ 错误日志。
- **原因**：SpecInfer 基于 Legion 图优化构建，底层算子融合开启 `--fusion` 后，对较新的 Ada 架构（sm_89）未经标定，产生了深度内存指针越界。
- **正解**：**不要拿没有经过论文验证的较新消费级显卡死磕。** 直接租用官方采用的 **A100 (sm_80)**、A10 等正规军算力卡，能够从根源消除所有的架构 C++ Bug。

---

### 2. 磁盘空间：AutoDL 默认 30GB 系统盘的无底洞
- **现象**：在跑 `download_models.sh` 或者 `basic_test.sh` 时，直接暴毙报 `OSError: Not enough free space` 或者系统崩溃无法 SSH。
- **原因**：HuggingFace 会在 `~/.cache/huggingface` 暴风吸入模型权重；FlexFlow 会在 `~/.cache/flexflow` 里把权重切成双倍体积的 FP16 张量；运行矩阵分割时，还会在 `/tmp` 产生数十 GB 碎图。
- **正解**：
  在一切开始之前，扩容数据盘（`/autodl-tmp` > 300GB），强制注射这四个续命环境变量：
  ```bash
  export HF_HOME=/autodl-tmp/.cache/huggingface
  export FF_CACHE_DIR=/autodl-tmp/.cache/flexflow
  export TMPDIR=/autodl-tmp/tmp
  export HF_ENDPOINT=https://hf-mirror.com  # 顺带加上国内大善人加速源
  ```

---

### 3. 底层依赖三剑客：UCX、MPI 与 Rust
- **现象**：运行 `./install_specinfer.sh` 编译 C++ 源码时，CMake 疯狂报错 `ucxConfig.cmake not found`、`Could NOT find MPI`、或者 `Rust is not installed`。
- **原因**：官方环境假定是在完整的分布式节点上，所以即便你是单卡单机，Legion 引擎也**必须**需要 UCX 和 MPI 的头文件和编译器支撑。此外 `tokenizers-c` 绑定使用了 Rust。
- **正解**：
  在 `install_specinfer.sh` **之前**：
  ```bash
  # 1. 装 UCX (按官网 README 里的隐藏步骤)
  ./install_ucx.sh && export UCX_DIR="$PWD/ucx-1.15.0/install"
  export LD_LIBRARY_PATH=$UCX_DIR/lib:$LD_LIBRARY_PATH
  export ucx_DIR=$UCX_DIR/lib/cmake/ucx
  
  # 2. 装 MPI (Ubuntu 和 Conda 双管齐下)
  apt-get install -y openmpi-bin libopenmpi-dev
  
  # 3. 装 Rust
  curl https://sh.rustup.rs -sSf | sh -s -- -y
  ```

---

### 4. Rust 版本过高导致 `tokenizers-c` 防御性编译失败
- **现象**：编译 Rust 子模块时报 `error: implicit autoref creates a reference to the dereference of a raw pointer`。
- **原因**：新版本的 Rustc (1.76+) 将早期 FlexFlow 第三方库中使用的“自动裸指针引用”列为不安全的断言拒绝编译。
- **正解**：
  用系统级替换直接修正旧版 C++代码的裸指针写法（加上 `&()`）：
  ```bash
  find /root/specinfer-ae -name "lib.rs" -exec sed -i 's/\*out_len = (\*handle)\.decode_str\.len();/\*out_len = (\&(\*handle)\.decode_str)\.len();/g' {} +
  ```

---

### 5. Python 环境大断层：`ModuleNotFoundError: No module named 'flexflow'`
- **现象**：C++ 经历了九九八十一难终于在 `make -j` 编译成功，结果运行验证测试或者模型下载时 Python 提示找不着 flexflow 和 legion_cffi。
- **原因**：FlexFlow 的包管理**不是通过 pip 打包的！** 它完全依赖临时环境变量将 Python 会话重定向到 `build` 和 `bindings` 目录。
- **正解**：
  这三行环境变量决定了你在 Python 能不能看见 C++ 的世界：
  ```bash
  export FF_HOME=/root/specinfer-ae/FlexFlow
  export LD_LIBRARY_PATH=$FF_HOME/build:$LD_LIBRARY_PATH
  export PYTHONPATH=$FF_HOME/python:$FF_HOME/build/python:$FF_HOME/deps/legion/bindings/python:$PYTHONPATH
  # 绑定 GPU 推理模式
  export FF_GPU_BACKEND=cuda
  ```

---

### 6. CFFI 粘合层生成：找不着 `legion_defines.h`
- **现象**：准备手动生成 Python 与底层 Legion 的桥接库（`python legion_cffi_build.py`）时，爆红提示 `legion_defines.h: No such file`。
- **原因**：CMake 在构建大树时，把这个带环境宏的头文件动态生成到了 `./build` 文件夹里，而 Python CFFI 绑定的默认脚本依然傻傻地去源码目录里找。
- **正解**：
  手动把生成在 `build` 里的头文件转移给 CFFI：
  ```bash
  cp ./FlexFlow/build/deps/legion/runtime/legion/legion_defines.h ./FlexFlow/deps/legion/runtime/legion/
  cp ./FlexFlow/build/deps/legion/runtime/legion/legion_config.h ./FlexFlow/deps/legion/runtime/legion/
  cd ./FlexFlow/deps/legion/bindings/python
  python legion_cffi_build.py
  ```

---

### 7. 野生模型的锅：`Can't load tokenizer`
- **现象**：利用 HF 脚本成功拖曳下载了 `JackFram/llama-68m`，却在最后关头因为初始化 Tokenizer 失败报错。
- **原因**：第三方用户（往往是草稿或剪枝模型作者）上传 HF 仓库时十分狂野，没把 Tokenizer 配置信息也打包进仓库。导致下载转换器卡死在预挂载环节。
- **正解**：
  同一个大系列的模型（例如 LLaMA-68M 与 LLaMA-7B），它们的分词器是共享同一套词表的。直接粗暴删去下载脚本中的校验，然后进行**移花接木**：
  ```bash
  sed -i '/AutoTokenizer.from_pretrained/d' ./FlexFlow/inference/utils/download_hf_model.py
  # 下载完再通过复制把大模型的分词器给小草稿模型
  cp -r /autodl-tmp/.cache/flexflow/weights/huggyllama/llama-7b/full-precision/tokenizer* /autodl-tmp/.cache/flexflow/weights/JackFram/llama-68m/full-precision/
  ```

---

**最终结论与行动指令（TL;DR）：**

以后在新机器配置 AE 实验，最高效的策略是：
1. 租 A100 + 400G 起步的数据盘。
2. 绝对先把所有环境变量和 `TMPDIR` 配好。
3. 把所有的依赖（MPI, UCX, Rust）提前打包装好。
4. 按序先编 C++，后把头文件挪回，生成好 python 的 `.so`。
5. 去掉巨无霸模型，屏蔽 Tokenizer 下载校验，加上国内镜像，直接起飞。
