# 毕设论文最终故事线与章节结构

## 一、先定论文命题

### 1. 最推荐的论文标题方向

以下三种都成立，优先推荐前两种：

1. **SpecInfer 的单卡复现与场景化评估：基于医疗问答与 Offloading 推理的实验研究**
2. **面向受限单卡环境的 SpecInfer 复现与应用迁移研究**
3. **SpecInfer 在医疗问答系统中的复现、评估与工程实现**

不建议使用的标题方向：

- “SpecInfer 在单卡常驻医疗问答中实现显著加速”
- “完整复现 SpecInfer 全部论文结果”
- “基于 SpecInfer 的医疗问答系统性能优化研究”

原因：

- 你当前最强正结果来自 `OPT-13B + OPT-125M` 的 **offloading** 路线，而不是单卡常驻 `LLaMA-7B`。
- 单卡常驻实验已经证明“机制可运行但未体现加速”，不能把它包装成主结论。

### 2. 论文核心论点

整篇论文建议围绕下面这句话展开：

> 本文复现了 SpecInfer 的核心推测推理机制，并在单卡受限环境下对“常驻推理”和“offloading 推理”两类场景进行了对比评估。实验表明，SpecInfer 在单卡常驻短回答场景中未观察到显著加速，但在 `OPT-13B` 的 offloading 场景下取得了稳定的端到端正向收益。

### 3. 本文不该主张的内容

以下内容不要写成你的结论：

- SpecInfer 在所有单卡场景都有效
- SpecInfer 在医疗问答系统中天然带来加速
- 本文完整复现了论文中分布式与多节点全部结果
- 本文证明 tree-based speculative inference 在当前单卡设置下一定优于 sequence-based

## 二、整篇论文的最终故事线

### 1. 故事主线

故事线建议固定为三层：

1. **问题层**
   大模型推理延迟高，尤其在资源受限部署和专业领域问答场景中，推理成本与响应速度成为系统落地障碍。

2. **方法层**
   SpecInfer 通过“小草稿模型 + 大目标模型”的推测式验证机制减少目标模型真实解码步数；其中论文主打的收益主要出现在：
   - 分布式推理
   - offloading 推理

3. **评估层**
   本文在单卡环境下进行复现，并比较两类运行区间：
   - 单卡常驻 `LLaMA-7B + 68M/160M`
   - 单卡 offloading `OPT-13B + OPT-125M`

最终发现：

- 常驻单卡下，SpecInfer 机制可运行，但 `verified token` 长期接近 `1`，没有转化为速度收益。
- offloading 场景下，SpecInfer 的 `verified token` 显著提高，并带来约 `3.84x - 3.93x` 的速度提升。

### 2. 论文亮点该怎么写

建议只保留三条贡献：

1. 在 AutoDL 单卡 A100 环境中完成了 SpecInfer 的工程复现，打通了模型下载、FlexFlow 构建、推理运行与实验采集全流程。
2. 系统比较了单卡常驻与 offloading 两类部署方式，解释了为何 SpecInfer 的收益依赖具体部署瓶颈。
3. 基于英文 prompt、医疗问答 workload 和 offloading 场景，构建了一套可复现实验流程，并给出了正向结果与负结果的统一解释。

## 三、推荐章节结构

## 第 1 章 绪论

### 1.1 研究背景

- 大模型推理成本高，延迟成为应用落地瓶颈
- 推测解码是近年的重要推理加速方向
- 但论文中的收益是否能在受限单卡环境下复现，仍需要工程验证

### 1.2 研究问题

建议明确提出三个问题：

1. SpecInfer 的核心机制能否在单卡环境中成功复现？
2. 在单卡常驻与单卡 offloading 两类场景下，SpecInfer 的收益是否一致？
3. 在医疗问答等应用迁移场景中，推测推理的效果与限制是什么？

### 1.3 研究内容

- 复现 SpecInfer AE
- 构建单卡实验脚本与数据记录机制
- 对比常驻与 offloading 场景
- 将结果映射到医疗问答和系统接入背景

### 1.4 主要贡献

这一节直接沿用上面的三条贡献即可。

## 第 2 章 相关工作

建议按四段来写：

### 2.1 推测解码与 Speculative Decoding

- Leviathan 等早期 speculative decoding
- 接受率、草稿长度与加速比理论

### 2.2 SpecInfer 与系统级推测推理

- token tree
- sequence-based 与 tree-based
- distributed / offloading 两类受益场景

### 2.3 RAG 与医疗问答

- 医疗问答中的检索增强
- 上下文长度、可信度、引用性问题

### 2.4 多智能体与系统协同

- 作为应用层背景即可，不宜写得过重
- 重点说明“上层业务协同”和“下层推理协同”是两个层次

注意：

- 你当前 [论文探讨.md](./论文探讨.md) 里 Medusa 的那条引用链接是错的，后面正式写作时必须修正。
- 相关工作不宜把太多篇幅放在多智能体，否则会冲淡本文主问题。

## 第 3 章 系统设计与复现方法

这一章非常重要，直接决定论文是否“像一篇复现与工程评估论文”。

建议拆成 4 节：

### 3.1 SpecInfer 机制概述

- `incremental decoding`
- `sequence-based speculative inference`
- `tree-based speculative inference`
- `verified tokens` 与加速关系

### 3.2 实验平台与运行环境

这一节直接引用你在 [问题回顾.md](./问题回顾.md) 里整理的踩坑与依赖：

- A100 单卡
- AutoDL 数据盘与缓存路径
- UCX / MPI / Rust / FlexFlow / Legion 依赖
- tokenizer 与缓存目录修复

这一节会非常加分，因为你已经有充分的一手复现问题材料。

### 3.3 单卡实验框架

分两条线描述：

1. **单卡常驻推理**
   - `LLaMA-7B + 68M/160M`
   - `incr / sequence / tree`

2. **单卡 offloading 推理**
   - `OPT-13B + OPT-125M`
   - baseline vs speculative

### 3.4 数据记录与实验脚本

- manifest 自动记录
- CSV 汇总
- latency / acceptance / verified token 提取

这一节要强调“复现实验可复用、可中断续跑、可导出结果”。

## 第 4 章 单卡常驻场景实验与分析

这一章写负结果，但要写得有解释力，不是简单承认失败。

### 4.1 实验设置

- 模型：`LLaMA-7B + LLaMA-68M`
- 批大小：`1/2/4`
- 模式：`incr / sequence / tree`
- 英文短 prompt 与 `len64` 医疗问答补充实验

### 4.2 结果

主表建议来自：

- `final_review_20260316_035130/server_gpu_single_a100/summary.csv`
- `final_review_20260316_035130/english_len64/latency_analysis_en/latency_summary.txt`

核心结果要写清楚：

- `verified_mean` 长期约等于 `1.0`
- `sequence/tree` latency 明显高于 `incr`
- 英文 `len64` 稳定，但未观察到 speedup

### 4.3 原因分析

这一节是关键：

1. 单卡常驻场景没有明显 CPU-GPU 参数搬运瓶颈
2. speculative 额外开销没有被更高的 verified token 数摊薄
3. 短回答和短 prompt 下，大模型本身解码代价不够高
4. 当前草稿模型在该任务分布下无法显著提高 verified token

### 4.4 小结

可以写成：

> 单卡常驻场景下，本文成功复现了 SpecInfer 的推测推理机制，但未观察到端到端加速。这表明 SpecInfer 的收益对部署方式与验证 token 数量高度敏感。

## 第 5 章 单卡 Offloading 场景实验与分析

这章是正文核心。

### 5.1 实验设置

- 模型：`OPT-13B`
- 草稿模型：`OPT-125M`
- 模式：
  - baseline offloading
  - speculative offloading
- 批大小：`1/2/4`
- 参数：
  - `CPUS=4`
  - `UTILS=4`
  - `ZSIZE=65000`
  - `MAX_SEQ_LEN=128`

### 5.2 端到端加速结果

主表直接来自：

- `final_review_20260316_035130/offloading/compare.csv`

建议正文直接写：

- `bs=1`: `3.85x`
- `bs=2`: `3.84x`
- `bs=4`: `3.93x`

### 5.3 机制解释：verified tokens

建议把下面这些统计写进正文：

- 平均 `verified tokens = 4.09`
- 中位数 `3`
- 最大值 `9`
- 大于 `1` 的比例约 `71.3%`

这一节的作用是把“为什么它快了”讲清楚，而不是只摆 speedup。

### 5.4 与常驻场景对比分析

建议写成一张对照表：

- 常驻单卡：
  - `verified ≈ 1`
  - 无加速
- offloading：
  - `verified > 1`
  - 明显加速

这张表会成为整篇论文最强的讨论点。

### 5.5 小结

直接写：

> 单卡 offloading 场景是本文中 SpecInfer 收益最明显的实验区间。实验表明，当目标模型推理瓶颈来自 CPU-GPU 参数调度时，SpecInfer 可以通过减少目标模型真实解码步数获得稳定的正向速度提升。

## 第 6 章 应用迁移：医疗问答与系统接入

这一章不要写成“医疗实验是主结果”，而是写成“应用迁移验证”。

### 6.1 英文医疗问答稳定性实验

使用：

- `final_review_20260316_035130/english_len64/stability_summary_en/failure_summary.txt`

核心写法：

- `50` 条中 `46` 条 `ok`
- `4` 条 `truncated`
- 说明推理链路稳定，但长度预算仍影响输出完整性

### 6.2 接受率代理指标

使用：

- `final_review_20260316_035130/english_len64/acceptance_analysis_en/acceptance_summary.txt`

要注意口径：

- 当前是 `output_agreement_proxy`
- 不是论文原始 acceptance rate

所以只能写成“输出一致性代理指标较高”，不能直接冒充论文 acceptance。

### 6.3 业务系统接入

如果你还有 FastAPI / OpenAI 兼容接口 / 本地前端打通的截图或请求响应，这一节就写成：

- `/v1/chat/completions` 接口设计
- 与业务端/Agent/RAG 的接入方式

如果你没有足够的演示材料，这一节就降级为“系统设计与接口方案”，不要写成充分实验。

## 第 7 章 复现难点、局限性与结论

这一章要同时承接 [问题回顾.md](./问题回顾.md) 和正向实验。

### 7.1 复现难点

可以直接整理你已有材料：

- CUDA/架构兼容
- 系统盘与缓存路径
- UCX / MPI / Rust / Legion / tokenizer
- AutoDL 环境限制

### 7.2 局限性

必须明确写：

1. 未复现分布式多节点结果
2. 单卡常驻 7B 场景未观察到加速
3. 医疗问答中的接受率指标仍主要是代理量
4. 部分系统接入验证偏工程演示，未进行大规模在线评估

### 7.3 结论

建议最终结论这样写：

> 本文完成了 SpecInfer 在单卡 A100 环境中的工程复现，并比较了单卡常驻与单卡 offloading 两类部署场景。实验结果表明，SpecInfer 在常驻短回答场景中未带来显著加速，但在 `OPT-13B` 的 offloading 场景中实现了约 `3.84x - 3.93x` 的稳定速度提升，说明其收益与系统瓶颈和验证 token 数密切相关。本文进一步给出了面向医疗问答与系统接入的应用迁移分析，为后续在受限资源环境下部署推测推理提供了工程经验与实证依据。

## 四、图表与表格安排建议

正文建议保留 4 张图/表即可：

1. **表 1：实验环境与模型配置**
2. **表 2：单卡常驻 `incr / sequence / tree` 对比**
3. **表 3：offloading baseline vs spec speedup**
4. **图 1：offloading speedup 柱状图**

可选正文图：

5. **图 2：英文医疗问答稳定性/接受率图**

附录建议放：

- 所有原始 `.out`
- 详细 prompt
- manifest
- 问题回顾与环境搭建细节

## 五、哪些内容建议删减

为了让论文聚焦，以下内容建议降权：

- 多智能体篇幅过长
- 医疗业务效果写得像主实验
- 过多展开 Java/前端工程细节
- 强行讨论 tree 在当前单卡下为什么一定优于 sequence

## 六、写作顺序建议

推荐按下面顺序写，不要从摘要开始：

1. 第 5 章 Offloading 主结果
2. 第 4 章 常驻负结果
3. 第 7 章 结论与局限
4. 第 3 章 复现方法与系统设计
5. 第 2 章 相关工作
6. 第 1 章 绪论
7. 最后写摘要

## 七、当前是否还需要新增实验

我的建议是：

- **不再新增单卡常驻实验**
- **不强制新增 OPT-30B 或 batch=8**
- 如果你缺系统接入截图，可以补 1 次最小演示

也就是说，当前已经足够进入论文正文写作阶段。

